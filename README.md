# 📘 Interview Questions (JavaScript)

## 1️⃣ What is JavaScript?
**JavaScript** is a scripting language that runs in the browser and on servers to create **dynamic and interactive applications**.

---

## 2️⃣ What is Hoisting in JavaScript?
**Hoisting** is JavaScript’s behavior of moving **declarations** to the top of their scope before code execution.

### Example (`var` hoisting)
```js
console.log(a);
var a = 10;
